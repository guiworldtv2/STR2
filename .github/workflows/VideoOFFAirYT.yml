name: LinkGrabber

on:
  schedule:
    - cron: '*/60 * * * *'

  pull_request:
    branches: [ main ]

  workflow_dispatch:
jobs:

  runCode:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Grab Link

        run: |
          git config --global user.email "fbk@hll.localhost"
          git config --global user.name "fbk"
          chmod +x VideoOFFAirYT.sh && ./VideoOFFAirYT.sh
          git add -A
          git commit -m "./VideoOFFAirYT.txt is updated."
          git push
